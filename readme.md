# Agent Console

This lowercase file exists for tooling that expects `readme.md`.

The canonical project documentation is in [README.md](./README.md).

Quick start:

```bash
npm install
npm run build
npm run start -w @agent-console/server
```

Host prerequisites are listed in [requirements.txt](./requirements.txt).
